\# Query Optimisation



Use indexes on frequently filtered fields such as candidate\_id.

Use EXPLAIN to inspect query plans and avoid unnecessary full table scans.

Limit database results and select only required columns.

